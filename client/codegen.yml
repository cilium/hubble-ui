schema:
  - "../server/src/schema.graphql"
documents: []
config:
  namingConvention:
    default: "change-case#pascalCase"
    enumValues: keep
generates:
  src/graphqlTypes.ts:
    config: {}
    plugins:
      - "typescript-common"
      - "typescript-client"
      - "typescript-server"
require: []
