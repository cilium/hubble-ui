schema:
  - "./src/types/schema.graphql"
documents: []
config:
  namingConvention:
    default: "change-case#pascalCase"
    enumValues: keep
generates:
  ./src/types/graphql.ts:
    config: {}
    plugins:
      - typescript
require: []
